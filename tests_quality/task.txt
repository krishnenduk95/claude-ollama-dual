Write a production-grade Python function `chunked(iterable, size)` that yields fixed-size chunks from an iterable. Requirements: (1) handle empty inputs, size<=0 edge cases; (2) memory-efficient (no materializing the full iterable); (3) preserve order; (4) work with generators not just lists; (5) include type hints and a brief docstring explaining complexity; (6) include 5 pytest test cases covering the edge cases. Respond with ONLY the final code file content as a single Python block — no prose, no markdown, ready to save as chunked.py. Aim for clarity over cleverness.
